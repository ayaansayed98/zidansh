# TODO: Make Whole Header Soft Pink

## Tasks
- [x] Change header's top offer bar from `bg-[#F5E5D5]` to `bg-pink-200` in Header.tsx
- [x] Change main header section from `bg-pink-100` to `bg-pink-200` in Header.tsx
